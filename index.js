/**
 * @format
 */

import {AppRegistry} from 'react-native';
import SoumFilter from './SoumFilter';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => SoumFilter);
